description = 'OpenRewrite recipes for migrating to the latest Form Engine Java API'

// All configuration is inherited from root build.gradle shared Java config.
// This project only contains YAML resources in META-INF/rewrite/.
