## Devapp

The [devapp](http://localhost:14000) is accessible after executing:

```sh
node --run start
```

### Editing models

While the devapp dev server is running, changes to models under
`exampleModels/src` are recompiled automatically (a Gradle continuous build runs
in the background) and the page reloads.
